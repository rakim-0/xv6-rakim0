To run this:

Firstly install all the build-essentials
sudo dnf install make automake gcc gcc-c++ kernel-devel git wget qemu gdb

1. Install gcc-10 using brew.
2. git clone https://github.com/rakim-0/xv6-public.git
3. cd xv6-public && make qemu-nox
